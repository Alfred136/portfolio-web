interface Styles {
  heading: string;
  subHeading: string;
  section: string;
}

export const styles: Styles = {
  heading: "text-[30px] xs:text-[45px] md:text-[60px] font-bold font-code",
  subHeading: "font-semi-bold text-[24px] xs:text-[30px] font-code",
  section: `w-full flex flex-col gap-10 sm:gap-20`,
};